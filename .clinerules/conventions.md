Don't add the state variables, useEffects, useCallbacks, unless absolutely have to. 
If adding state variables or refs, keep the amount to the minimum
Group css classes together where makes sense, instead of duplicating the same code